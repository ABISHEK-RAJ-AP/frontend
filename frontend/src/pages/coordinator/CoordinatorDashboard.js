import React from 'react';
import PlaceholderDashboard from '../common/PlaceholderDashboard';

const CoordinatorDashboard = () => <PlaceholderDashboard role="Coordinator" />;

export default CoordinatorDashboard;
