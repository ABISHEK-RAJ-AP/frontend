import React from 'react';

const PlaceholderDashboard = ({ role }) => (
  <div style={{ padding: '2rem' }}>
    <h1>{role} Dashboard</h1>
    <p>This is a placeholder page for {role}.</p>
  </div>
);

export default PlaceholderDashboard;
