import React from 'react';
import PlaceholderDashboard from '../common/PlaceholderDashboard';

const FacilitatorDashboard = () => <PlaceholderDashboard role="Facilitator" />;

export default FacilitatorDashboard;
